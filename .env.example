# Logpilot 会自动加载「当前工作目录」下的 .env
# 将本文件复制为 .env 后按需填写即可：
#   cp .env.example .env
#
# ================== 最小可用（推荐先跑起来） ==================
# 默认使用 OpenAI 兼容 API（LOGPILOT_PROVIDER 默认 openai）
# 只需要二选一填写 Key：
LOGPILOT_API_KEY=sk-your-key
# OPENAI_API_KEY=sk-your-key

# ================== 提供方（可选） ==================
# openai   - OpenAI 或任意 OpenAI 兼容网关（默认）
# deepseek - DeepSeek 官方 API（同样走 OpenAI 兼容的 /v1/chat/completions）
# LOGPILOT_PROVIDER=openai

# ================== API Base（可选） ==================
# Base URL 优先级（两者同时填时 LOGPILOT_API_BASE 优先）：
#   1) LOGPILOT_API_BASE
#   2) OPENAI_API_BASE_URL
#
# openai 默认：   https://api.openai.com/v1
# deepseek 默认： https://api.deepseek.com/v1
#
# LOGPILOT_API_BASE=https://api.openai.com/v1
# OPENAI_API_BASE_URL=https://api.openai.com/v1

# ================== 模型（可选） ==================
# openai 默认：   gpt-4o-mini
# deepseek 默认： deepseek-chat
# LOGPILOT_MODEL=gpt-4o-mini

# ================== DeepSeek Key（可选） ==================
# 当 LOGPILOT_PROVIDER=deepseek 时，Key 优先级：
#   1) DEEPSEEK_API_KEY
#   2) LOGPILOT_API_KEY
#   3) OPENAI_API_KEY
# DEEPSEEK_API_KEY=sk-your-deepseek-key
